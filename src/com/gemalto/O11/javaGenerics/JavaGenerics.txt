Java Generics
==============

Introduced in Java 5

To avoid type casting issues when fetching the items.
Avoids runtime error while fetching (ClassCastException)

Moving runtime -> compile time error

Ensures what type of object we are going to be storing
Warning for Raw types

items.add(1);
items.add("Archit");
items.add(2)



* Argument that can be passed in the Type parameter can be a Class or Interface
Interface itself could specify Typed parameter

Single Bound
Multiple Bound

public class Team<T extends Player & Coach & Manager> {
    ....
}

Player = class (first) OR Error
Coach = interface
Manager = interface
